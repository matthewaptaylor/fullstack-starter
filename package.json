{
  "name": "fullstack-starter",
  "version": "0.0.1",
  "description": "",
  "author": "Matthew Taylor",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "gen:axios": "openapi-generator-cli generate -g typescript-axios -i ./api/openapi.json -o ./axios-api-client --additional-properties=npmName=fullstack-starter-axios-api-client,supportsES6=true",
    "commitlint": "commitlint -e $GIT_PARAMS",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.6.7",
    "@commitlint/config-conventional": "^17.6.7",
    "@openapitools/openapi-generator-cli": "^2.7.0",
    "husky": "^8.0.3"
  }
}
